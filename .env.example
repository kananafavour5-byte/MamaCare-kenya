# Copy this file to .env and fill in your own key.
# Get a free key from Google Cloud Console (enable "Places API (New)").
# Restrict it to your local dev origin (http://localhost:5173) before using
# it anywhere public.
VITE_GOOGLE_PLACES_API_KEY=
